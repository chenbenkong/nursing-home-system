<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursinghome.mapper.EmployeeMapper">

    <resultMap id="EmployeeResultMap" type="com.nursinghome.entity.Employee">
        <id property="id" column="id"/>
        <result property="empNo" column="emp_no"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="birthDate" column="birth_date"/>
        <result property="idCard" column="id_card"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="address" column="address"/>
        <result property="department" column="department"/>
        <result property="position" column="position"/>
        <result property="jobTitle" column="job_title"/>
        <result property="education" column="education"/>
        <result property="entryDate" column="entry_date"/>
        <result property="leaveDate" column="leave_date"/>
        <result property="status" column="status"/>
        <result property="salary" column="salary"/>
        <result property="photo" column="photo"/>
        <result property="userId" column="user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <select id="selectById" resultMap="EmployeeResultMap">
        SELECT * FROM employee WHERE id = #{id}
    </select>

    <select id="selectByEmpNo" resultMap="EmployeeResultMap">
        SELECT * FROM employee WHERE emp_no = #{empNo}
    </select>

    <select id="selectByIdCard" resultMap="EmployeeResultMap">
        SELECT * FROM employee WHERE id_card = #{idCard}
    </select>

    <select id="selectAll" resultMap="EmployeeResultMap">
        SELECT * FROM employee ORDER BY create_time DESC
    </select>

    <select id="selectByDepartment" resultMap="EmployeeResultMap">
        SELECT * FROM employee WHERE department = #{department} ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" resultMap="EmployeeResultMap">
        SELECT * FROM employee WHERE status = #{status} ORDER BY create_time DESC
    </select>

    <select id="selectByCondition" resultMap="EmployeeResultMap">
        SELECT * FROM employee
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="department != null and department != ''">
                AND department = #{department}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO employee (emp_no, name, gender, birth_date, id_card, phone, email, address,
                             department, position, job_title, education, entry_date, status,
                             salary, photo, user_id, remark)
        VALUES (#{empNo}, #{name}, #{gender}, #{birthDate}, #{idCard}, #{phone}, #{email}, #{address},
                #{department}, #{position}, #{jobTitle}, #{education}, #{entryDate}, #{status},
                #{salary}, #{photo}, #{userId}, #{remark})
    </insert>

    <update id="update">
        UPDATE employee
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthDate != null">birth_date = #{birthDate},</if>
            <if test="idCard != null">id_card = #{idCard},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="address != null">address = #{address},</if>
            <if test="department != null">department = #{department},</if>
            <if test="position != null">position = #{position},</if>
            <if test="jobTitle != null">job_title = #{jobTitle},</if>
            <if test="education != null">education = #{education},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="leaveDate != null">leave_date = #{leaveDate},</if>
            <if test="status != null">status = #{status},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="photo != null">photo = #{photo},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="remark != null">remark = #{remark}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM employee WHERE id = #{id}
    </delete>

    <select id="countByDepartment" resultType="java.lang.Long">
        SELECT COUNT(*) FROM employee 
        <where>
            <if test="department != null">
                department = #{department}
            </if>
        </where>
    </select>

    <select id="countByStatus" resultType="java.lang.Long">
        SELECT COUNT(*) FROM employee 
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
    </select>

</mapper>
