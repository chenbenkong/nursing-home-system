# 养老院管理系统 - 启动步骤

## 你的环境（已检测）
- ✅ Java 17
- ✅ Node.js 22
- ✅ Maven 3.9
- ⚠️ MySQL 80（需要手动启动）

---

## 第一步：启动 MySQL 服务

以**管理员身份**打开 PowerShell，执行：
```powershell
Start-Service MySQL80
```

或者手动启动：
1. 按 `Win + R`，输入 `services.msc`
2. 找到 **MySQL80** 服务
3. 右键点击 **启动**

---

## 第二步：创建数据库

以**管理员身份**打开 PowerShell，执行：
```powershell
cd nursing-home-system
cat database/nursing_home.sql | mysql -u root -p
```

然后输入你的 MySQL root 密码。

**或者分步执行：**
```powershell
# 1. 进入 MySQL
mysql -u root -p

# 2. 创建数据库（在 MySQL 命令行中执行）
CREATE DATABASE IF NOT EXISTS nursing_home DEFAULT CHARACTER SET utf8mb4;

# 3. 退出
exit

# 4. 导入数据
mysql -u root -p nursing_home < database/nursing_home.sql
```

---

## 第三步：修改数据库配置

打开文件：`backend/src/main/resources/jdbc.properties`

修改为你的 MySQL 用户名和密码：
```properties
jdbc.username=你的用户名
jdbc.password=你的密码
```

---

## 第四步：启动后端

打开 PowerShell，执行：
```powershell
cd nursing-home-system/backend
mvn tomcat7:run
```

等待看到 `Tomcat started` 表示启动成功！

**访问地址**：http://localhost:8081

---

## 第五步：启动前端

**新开一个 PowerShell 窗口**，执行：
```powershell
cd nursing-home-system/frontend
npm install        # 第一次需要安装依赖
npm run serve
```

等待看到 `App running at: http://localhost:8080` 表示启动成功！

---

## 第六步：访问系统

打开浏览器访问：**http://localhost:8080**

**默认账号**：
- 用户名：`admin`
- 密码：`admin123`

---

## 常见问题

### 1. MySQL 启动失败
- 检查 MySQL 是否正确安装
- 检查 MySQL 服务名是否为 MySQL80

### 2. 数据库导入失败
- 检查 MySQL 是否已启动
- 检查用户名密码是否正确

### 3. 后端启动失败
- 检查数据库配置是否正确
- 检查 8081 端口是否被占用

### 4. 前端启动失败
- 检查 Node.js 是否安装
- 检查 8080 端口是否被占用
