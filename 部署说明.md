# 养老院管理系统 - Docker部署说明

## 最简单部署步骤（3步完成）

### 第1步：构建项目

在本地Windows电脑上执行：

```bash
# 1. 构建后端
cd backend
mvn clean package -DskipTests

# 2. 构建前端
cd ../frontend
npm install
npm run build
```

---

### 第2步：上传到宝塔

**将整个项目文件夹上传到服务器：**
```
/www/wwwroot/nursing-home-system/
├── backend/
│   ├── Dockerfile
│   └── target/nursing-home-system.war
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── dist/
└── docker-compose.yml
```

---

### 第3步：宝塔一键部署

1. **连接MySQL容器到项目网络**
   ```bash
   # 在宝塔终端执行
   docker network create nursing-network
   docker network connect nursing-network mysql
   ```

2. **启动项目**
   ```bash
   cd /www/wwwroot/nursing-home-system
   docker-compose up -d
   ```

   或者使用宝塔面板：
   - 宝塔 → Docker → Compose
   - 点击"添加项目"
   - 选择目录：`/www/wwwroot/nursing-home-system`
   - 点击"启动"

---

## 完成！

访问地址：`http://你的服务器IP`

---

## 常用操作

### 查看运行状态
```bash
docker ps
```

### 查看日志
```bash
# 查看后端日志
docker logs nursing-tomcat

# 查看前端日志
docker logs nursing-nginx
```

### 重启服务
```bash
cd /www/wwwroot/nursing-home-system
docker-compose restart
```

### 停止服务
```bash
cd /www/wwwroot/nursing-home-system
docker-compose down
```

### 重新部署（更新代码后）
```bash
cd /www/wwwroot/nursing-home-system
# 1. 重新构建
docker-compose build

# 2. 重启
docker-compose up -d
```

---

## 配置说明

### 数据库配置（已配置好）
- 主机：`mysql`（Docker容器名）
- 端口：`3306`
- 用户名：`root`
- 密码：`1234`
- 数据库：`nursing_home`

### 网络配置
- 项目使用外部网络 `nursing-network`
- 你的MySQL容器需要连接到这个网络才能通信

---

## 注意事项

1. **确保MySQL容器已启动**
   ```bash
   docker ps | grep mysql
   ```

2. **确保MySQL在网络中**
   ```bash
   docker network inspect nursing-network
   ```
   应该能看到 `mysql` 容器

3. **防火墙**：宝塔和腾讯云安全组都要放行80端口

4. **数据库**：确保 `nursing_home` 数据库已创建
   ```sql
   CREATE DATABASE IF NOT EXISTS nursing_home CHARACTER SET utf8mb4;
   ```
