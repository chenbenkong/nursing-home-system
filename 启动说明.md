# 养老院管理系统 - 启动说明

## 环境要求

1. **JDK 11** 或更高版本
2. **MySQL 8.0** 或更高版本
3. **Node.js 16** 或更高版本
4. **Maven 3.6** 或更高版本
5. **Tomcat 9** 或更高版本（可选，用于部署后端）

## 数据库配置

### 1. 创建数据库

```bash
mysql -u root -p
```

在MySQL中执行：
```sql
CREATE DATABASE IF NOT EXISTS nursing_home 
DEFAULT CHARACTER SET utf8mb4 
DEFAULT COLLATE utf8mb4_unicode_ci;
```

### 2. 导入数据

```bash
mysql -u root -p nursing_home < database/nursing_home.sql
```

### 3. 修改数据库连接配置

编辑 `backend/src/main/resources/jdbc.properties`：

```properties
jdbc.driver=com.mysql.cj.jdbc.Driver
jdbc.url=jdbc:mysql://localhost:3306/nursing_home?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
jdbc.username=你的用户名
jdbc.password=你的密码
```

## 启动后端

### 方式一：使用IDEA/Eclipse（推荐开发时使用）

1. 导入backend项目作为Maven项目
2. 配置Tomcat服务器
3. 运行项目

### 方式二：命令行编译部署

```bash
cd backend
mvn clean package
```

将生成的 `target/nursing-home-system.war` 复制到Tomcat的webapps目录，然后启动Tomcat。

后端默认访问地址：`http://localhost:8080/nursing-home-system`

## 启动前端

```bash
cd frontend
npm install
npm run serve
```

前端默认访问地址：`http://localhost:8080`

## 登录信息

- **用户名**: admin
- **密码**: admin123

## 常见问题

### 1. 数据库连接失败
- 检查MySQL服务是否启动
- 检查jdbc.properties中的用户名密码是否正确
- 检查数据库 nursing_home 是否已创建

### 2. 跨域问题
前端已配置代理，开发时自动解决跨域问题。生产环境需要配置Nginx或修改后端CORS配置。

### 3. 端口冲突
- 前端默认端口：8080
- 后端默认端口：8080（Tomcat）

如果端口冲突，可以修改：
- 前端：`frontend/vue.config.js` 中的 devServer.port
- 后端：Tomcat的server.xml配置

## 项目验证

启动成功后，访问 http://localhost:8080 应该能看到登录页面。

输入 admin / admin123 登录后，可以看到仪表盘页面，显示老人数量、员工数量等统计数据。
