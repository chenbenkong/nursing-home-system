<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursinghome.mapper.MedicineMapper">

    <resultMap id="BaseResultMap" type="com.nursinghome.entity.Medicine">
        <id column="id" property="id"/>
        <result column="medicine_no" property="medicineNo"/>
        <result column="name" property="name"/>
        <result column="category" property="category"/>
        <result column="specification" property="specification"/>
        <result column="manufacturer" property="manufacturer"/>
        <result column="batch_no" property="batchNo"/>
        <result column="expiry_date" property="expiryDate"/>
        <result column="stock_quantity" property="stockQuantity"/>
        <result column="unit" property="unit"/>
        <result column="price" property="price"/>
        <result column="storage_condition" property="storageCondition"/>
        <result column="usage_method" property="usageMethod"/>
        <result column="contraindication" property="contraindication"/>
        <result column="side_effects" property="sideEffects"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, medicine_no, name, category, specification, manufacturer, batch_no, expiry_date,
        stock_quantity, unit, price, storage_condition, usage_method, contraindication, side_effects,
        status, create_time, update_time, remark
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM medicine
        ORDER BY create_time DESC
    </select>

    <select id="selectByCategory" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM medicine
        WHERE category = #{category}
        ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM medicine
        WHERE status = #{status}
        ORDER BY create_time DESC
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM medicine
        WHERE name LIKE CONCAT('%', #{name}, '%')
        ORDER BY create_time DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM medicine
        WHERE id = #{id}
    </select>

    <select id="selectByMedicineNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM medicine
        WHERE medicine_no = #{medicineNo}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO medicine (
            medicine_no, name, category, specification, manufacturer, batch_no, expiry_date,
            stock_quantity, unit, price, storage_condition, usage_method, contraindication, side_effects,
            status, remark
        ) VALUES (
            #{medicineNo}, #{name}, #{category}, #{specification}, #{manufacturer}, #{batchNo}, #{expiryDate},
            #{stockQuantity}, #{unit}, #{price}, #{storageCondition}, #{usageMethod}, #{contraindication}, #{sideEffects},
            #{status}, #{remark}
        )
    </insert>

    <update id="update">
        UPDATE medicine
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="category != null">category = #{category},</if>
            <if test="specification != null">specification = #{specification},</if>
            <if test="manufacturer != null">manufacturer = #{manufacturer},</if>
            <if test="batchNo != null">batch_no = #{batchNo},</if>
            <if test="expiryDate != null">expiry_date = #{expiryDate},</if>
            <if test="stockQuantity != null">stock_quantity = #{stockQuantity},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="price != null">price = #{price},</if>
            <if test="storageCondition != null">storage_condition = #{storageCondition},</if>
            <if test="usageMethod != null">usage_method = #{usageMethod},</if>
            <if test="contraindication != null">contraindication = #{contraindication},</if>
            <if test="sideEffects != null">side_effects = #{sideEffects},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM medicine WHERE id = #{id}
    </delete>

    <select id="countAll" resultType="java.lang.Long">
        SELECT COUNT(*) FROM medicine
    </select>

    <select id="countByCategory" resultType="java.lang.Long">
        SELECT COUNT(*) FROM medicine WHERE category = #{category}
    </select>

    <select id="countByStatus" resultType="java.lang.Long">
        SELECT COUNT(*) FROM medicine WHERE status = #{status}
    </select>

    <select id="selectLowStock" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM medicine
        WHERE stock_quantity &lt;= 10 AND status != 'DISABLED'
        ORDER BY stock_quantity ASC
    </select>

    <select id="selectExpired" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM medicine
        WHERE expiry_date &lt; CURDATE() AND status != 'DISABLED'
        ORDER BY expiry_date ASC
    </select>

</mapper>
