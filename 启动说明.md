# 养老院管理系统 - 详细启动说明

> 本文档面向零基础用户，请按照步骤一步步操作。

## 📋 目录
1. [环境准备](#环境准备)
2. [数据库安装配置](#数据库安装配置)
3. [项目启动](#项目启动)
4. [常见问题解决](#常见问题解决)

---

## 环境准备

### 1. 需要安装的软件

| 软件 | 版本要求 | 用途 | 下载地址 |
|------|---------|------|---------|
| JDK | 17+ | Java运行环境 | https://adoptium.net/ |
| MySQL | 8.0+ | 数据库 | https://dev.mysql.com/downloads/mysql/ |
| Node.js | 16+ | 前端运行环境 | https://nodejs.org/ (选LTS版本) |
| Maven | 3.6+ | 项目构建工具 | https://maven.apache.org/download.cgi |
| Git | 最新版 | 代码管理 | https://git-scm.com/downloads |

### 2. 验证安装是否成功

打开命令提示符（CMD），分别输入以下命令检查：

```bash
# 检查Java版本
java -version
# 应该显示类似：openjdk version "17.0.x"

# 检查Maven版本
mvn -version
# 应该显示类似：Apache Maven 3.9.x

# 检查Node版本
node -v
# 应该显示类似：v18.x.x

# 检查MySQL版本
mysql --version
# 应该显示类似：mysql Ver 8.0.x
```

如果以上命令都显示版本号，说明环境准备完成！

---

## 数据库安装配置

### 1. 启动MySQL服务

**Windows用户：**
- 按 `Win + R`，输入 `services.msc` 回车
- 找到 `MySQL80` 服务，确保状态为"正在运行"
- 如果没有运行，右键点击选择"启动"

**Mac/Linux用户：**
```bash
# 启动MySQL服务
sudo systemctl start mysql
# 或
brew services start mysql
```

### 2. 创建数据库

**方式一：使用MySQL命令行（推荐）**

```bash
# 登录MySQL（会提示输入密码）
mysql -u root -p

# 在MySQL命令行中执行以下命令：
CREATE DATABASE IF NOT EXISTS nursing_home 
DEFAULT CHARACTER SET utf8mb4 
DEFAULT COLLATE utf8mb4_unicode_ci;

# 查看数据库是否创建成功
SHOW DATABASES;

# 退出MySQL
EXIT;
```

**方式二：使用Navicat等图形化工具**

1. 打开Navicat，连接到你的MySQL服务器
2. 右键点击左侧的"连接名"
3. 选择"新建数据库"
4. 数据库名填写：`nursing_home`
5. 字符集选择：`utf8mb4`
6. 排序规则选择：`utf8mb4_unicode_ci`
7. 点击"确定"

### 3. 导入数据库表结构和数据

**方式一：使用命令行**

```bash
# 进入项目目录
cd nursing-home-system

# 导入数据库（会提示输入密码）
mysql -u root -p nursing_home < database/nursing_home.sql
```

**方式二：使用Navicat**

1. 双击打开 `nursing_home` 数据库
2. 右键点击数据库名，选择"运行SQL文件"
3. 选择项目目录下的 `database/nursing_home.sql` 文件
4. 点击"开始"
5. 等待导入完成

### 4. 修改数据库连接配置

找到并编辑文件：`backend/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    # 数据库连接地址（一般不用改）
    url: jdbc:mysql://localhost:3306/nursing_home?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    # 修改为你的MySQL用户名（通常是root）
    username: root
    # 修改为你的MySQL密码
    password: 你的密码
```

**注意：**
- 如果MySQL端口不是3306，请修改url中的端口号
- 确保用户名和密码正确，否则后端无法连接数据库

---

## 项目启动

### 方式一：快速启动（Windows推荐）

**适合：** 已经配置好数据库的Windows用户

1. 双击运行项目根目录下的 `快速启动.bat` 文件
2. 等待脚本执行完成（大约需要30秒）
3. 会自动打开两个命令行窗口：
   - 后端服务窗口（标题：后端服务）
   - 前端服务窗口（标题：前端服务）
4. 打开浏览器访问：http://localhost:8080

**登录账号：**
- 用户名：`admin`
- 密码：`123456`

### 方式二：手动启动（适合所有系统）

#### 步骤1：启动后端服务

```bash
# 进入后端目录
cd backend

# 编译并启动（第一次会比较慢，需要下载依赖）
mvn spring-boot:run

# 看到类似以下信息表示启动成功：
# "Started NursingHomeApplication in x.x seconds"
# "Tomcat started on port(s): 8081"
```

**后端访问地址：** http://localhost:8081

**常见错误：**
- 如果提示端口8081被占用，修改 `application.yml` 中的 `server.port`
- 如果提示数据库连接失败，检查数据库配置是否正确

#### 步骤2：启动前端服务

**新开一个命令行窗口**，执行：

```bash
# 进入前端目录
cd frontend

# 安装依赖（第一次需要，以后不需要）
npm install

# 启动前端服务
npm run serve

# 看到类似以下信息表示启动成功：
# "App running at:"
# "- Local:   http://localhost:8080/"
```

**前端访问地址：** http://localhost:8080

**常见错误：**
- 如果提示端口8080被占用，修改 `vue.config.js` 中的 `devServer.port`

### 方式三：Docker部署（适合有Docker经验的用户）

```bash
# 确保已安装Docker和Docker Compose

# 在项目根目录执行
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

---

## 常见问题解决

### ❌ 问题1：后端启动报错 "Name for argument of type [java.lang.String] not specified"

**原因：** Maven编译缓存问题

**解决：**
```bash
cd backend
mvn clean compile
mvn spring-boot:run
```

### ❌ 问题2：数据库连接失败

**检查清单：**
1. MySQL服务是否启动？
   - Windows：`services.msc` 查看MySQL80服务状态
2. 用户名密码是否正确？
   - 尝试用命令行登录：`mysql -u root -p`
3. 数据库是否创建？
   - 登录MySQL执行：`SHOW DATABASES;` 查看是否有nursing_home
4. 端口是否正确？
   - 默认3306，如果不是请修改application.yml

### ❌ 问题3：前端安装依赖失败

**解决：**
```bash
# 清除npm缓存
npm cache clean --force

# 使用淘宝镜像加速
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

### ❌ 问题4：端口被占用

**查看端口占用：**
```bash
# Windows
netstat -ano | findstr :8080

# Mac/Linux
lsof -i :8080
```

**修改端口：**
- 后端端口：`backend/src/main/resources/application.yml` 修改 `server.port`
- 前端端口：`frontend/vue.config.js` 修改 `devServer.port`

### ❌ 问题5：页面显示空白或报错

**检查：**
1. 后端是否启动成功？访问 http://localhost:8081 看是否有响应
2. 前端是否启动成功？查看命令行是否有错误信息
3. 浏览器控制台是否有报错？按F12打开开发者工具查看

### ❌ 问题6：登录失败

**检查：**
1. 确认访问的是前端地址 http://localhost:8080
2. 确认数据库已导入初始数据
3. 检查后端日志是否有错误

---

## 🔧 开发常用命令

### 后端开发

```bash
cd backend

# 编译项目
mvn clean compile

# 运行测试
mvn test

# 打包（生成jar文件）
mvn clean package -DskipTests

# 运行打包后的项目
java -jar target/nursing-home-system-1.0.0.jar
```

### 前端开发

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run serve

# 构建生产环境代码
npm run build

# 代码检查
npm run lint
```

---

## 📞 技术支持

如果遇到问题无法解决：
1. 查看后端控制台日志（黑色窗口）
2. 查看前端控制台日志（按F12打开浏览器开发者工具）
3. 检查本文档的"常见问题解决"部分

---

## ✅ 启动检查清单

启动前请确认：
- [ ] MySQL服务已启动
- [ ] 数据库 `nursing_home` 已创建
- [ ] 数据库表结构已导入
- [ ] `application.yml` 中的数据库密码已修改
- [ ] JDK 17+ 已安装
- [ ] Maven 3.6+ 已安装
- [ ] Node.js 16+ 已安装

---

**最后更新时间：** 2026-03-12
