<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursinghome.mapper.ActivityMapper">

    <resultMap id="BaseResultMap" type="com.nursinghome.entity.Activity">
        <id column="id" property="id"/>
        <result column="activity_no" property="activityNo"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="activity_type" property="activityType"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="location" property="location"/>
        <result column="max_participants" property="maxParticipants"/>
        <result column="current_participants" property="currentParticipants"/>
        <result column="organizer_id" property="organizerId"/>
        <result column="organizer_name" property="organizerName"/>
        <result column="status" property="status"/>
        <result column="cover_image" property="coverImage"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, activity_no, title, content, activity_type, start_time, end_time, location,
        max_participants, current_participants, organizer_id, organizer_name, status, cover_image, create_time, update_time, remark
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM activity
        ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM activity
        WHERE status = #{status}
        ORDER BY start_time ASC
    </select>

    <select id="selectByActivityType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM activity
        WHERE activity_type = #{activityType}
        ORDER BY create_time DESC
    </select>

    <select id="selectByOrganizerId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM activity
        WHERE organizer_id = #{organizerId}
        ORDER BY create_time DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM activity
        WHERE id = #{id}
    </select>

    <select id="selectByActivityNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM activity
        WHERE activity_no = #{activityNo}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO activity (
            activity_no, title, content, activity_type, start_time, end_time, location,
            max_participants, current_participants, organizer_id, organizer_name, status, cover_image, remark
        ) VALUES (
            #{activityNo}, #{title}, #{content}, #{activityType}, #{startTime}, #{endTime}, #{location},
            #{maxParticipants}, #{currentParticipants}, #{organizerId}, #{organizerName}, #{status}, #{coverImage}, #{remark}
        )
    </insert>

    <update id="update">
        UPDATE activity
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="activityType != null">activity_type = #{activityType},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="location != null">location = #{location},</if>
            <if test="maxParticipants != null">max_participants = #{maxParticipants},</if>
            <if test="currentParticipants != null">current_participants = #{currentParticipants},</if>
            <if test="organizerId != null">organizer_id = #{organizerId},</if>
            <if test="organizerName != null">organizer_name = #{organizerName},</if>
            <if test="status != null">status = #{status},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="remark != null">remark = #{remark}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM activity WHERE id = #{id}
    </delete>

    <update id="incrementCurrentParticipants">
        UPDATE activity SET current_participants = current_participants + 1 WHERE id = #{id}
    </update>

    <update id="decrementCurrentParticipants">
        UPDATE activity SET current_participants = current_participants - 1 WHERE id = #{id}
    </update>

    <select id="countAll" resultType="java.lang.Long">
        SELECT COUNT(*) FROM activity
    </select>

    <select id="countByStatus" resultType="java.lang.Long">
        SELECT COUNT(*) FROM activity WHERE status = #{status}
    </select>

</mapper>
