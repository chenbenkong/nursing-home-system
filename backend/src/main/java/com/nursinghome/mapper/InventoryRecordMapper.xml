<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursinghome.mapper.InventoryRecordMapper">

    <resultMap id="BaseResultMap" type="com.nursinghome.entity.InventoryRecord">
        <id column="id" property="id"/>
        <result column="item_id" property="itemId"/>
        <result column="item_name" property="itemName"/>
        <result column="record_type" property="recordType"/>
        <result column="quantity" property="quantity"/>
        <result column="before_stock" property="beforeStock"/>
        <result column="after_stock" property="afterStock"/>
        <result column="reason" property="reason"/>
        <result column="related_id" property="relatedId"/>
        <result column="related_name" property="relatedName"/>
        <result column="operator_id" property="operatorId"/>
        <result column="operator_name" property="operatorName"/>
        <result column="operate_time" property="operateTime"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, item_id, item_name, record_type, quantity, before_stock, after_stock,
        reason, related_id, related_name, operator_id, operator_name, operate_time, remark
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM inventory_record
        ORDER BY operate_time DESC
    </select>

    <select id="selectByItemId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM inventory_record
        WHERE item_id = #{itemId}
        ORDER BY operate_time DESC
    </select>

    <select id="selectByRecordType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM inventory_record
        WHERE record_type = #{recordType}
        ORDER BY operate_time DESC
    </select>

    <select id="selectByOperatorId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM inventory_record
        WHERE operator_id = #{operatorId}
        ORDER BY operate_time DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM inventory_record
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO inventory_record (
            item_id, item_name, record_type, quantity, before_stock, after_stock,
            reason, related_id, related_name, operator_id, operator_name, operate_time, remark
        ) VALUES (
            #{itemId}, #{itemName}, #{recordType}, #{quantity}, #{beforeStock}, #{afterStock},
            #{reason}, #{relatedId}, #{relatedName}, #{operatorId}, #{operatorName}, #{operateTime}, #{remark}
        )
    </insert>

    <update id="update">
        UPDATE inventory_record
        <set>
            <if test="itemId != null">item_id = #{itemId},</if>
            <if test="itemName != null">item_name = #{itemName},</if>
            <if test="recordType != null">record_type = #{recordType},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="beforeStock != null">before_stock = #{beforeStock},</if>
            <if test="afterStock != null">after_stock = #{afterStock},</if>
            <if test="reason != null">reason = #{reason},</if>
            <if test="relatedId != null">related_id = #{relatedId},</if>
            <if test="relatedName != null">related_name = #{relatedName},</if>
            <if test="operatorId != null">operator_id = #{operatorId},</if>
            <if test="operatorName != null">operator_name = #{operatorName},</if>
            <if test="operateTime != null">operate_time = #{operateTime},</if>
            <if test="remark != null">remark = #{remark}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM inventory_record WHERE id = #{id}
    </delete>

    <select id="countAll" resultType="java.lang.Long">
        SELECT COUNT(*) FROM inventory_record
    </select>

    <select id="countByItemId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM inventory_record WHERE item_id = #{itemId}
    </select>

    <select id="countByRecordType" resultType="java.lang.Long">
        SELECT COUNT(*) FROM inventory_record WHERE record_type = #{recordType}
    </select>

</mapper>
